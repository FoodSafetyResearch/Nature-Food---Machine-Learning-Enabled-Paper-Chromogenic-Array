'Python_code' is the NN algorithm.
'Data_test' is the testing dataset for the lettuce experiment.*
'Data_train' is the training dataset for the lettuce experiment.*
'Sample_output' fold includes sample training results over 15,000 iterations.

*In the training and testing dataset, column A is the sample code, column B is the replicate number, and columns C-BM are the digitized R/G/B data. 

